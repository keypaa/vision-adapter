# Probe 4000 Roadmap Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Finish the Qwen3.5-2B vision-alignment probe (4000 steps, 64k samples) on Molab PRO 6000 96GB — never on B300 — with the memory budget contract pinned, GPU-validated, then run bench + FlexAttention prep.

**Architecture:** 6 sequential phases with numeric gates. CPU first (P0 contract pins, no behavior change), then Molab GPU (worst-shard + gated resume), then the 4000 run, then a data-driven L-split decision (only if GPU proves micro=1 OOMs), then bench/FlexAttention per docs/NEXT_STEPS.md §3.

**Tech Stack:** Python, torch, pytest, ruff, Molab RTX PRO 6000 96GB, HuggingFace Hub.

## Global Constraints

- Branch: `feat/bucketed-hf-streaming`, baseline `bfab5a1` (grad-accum fix + 102 tests green).
- Commit gate per commit: `ruff check` clean on touched files (the 5 pre-existing errors in `vision_adapter/train.py` stay untouched) + `python -m py_compile` + `pytest -q` green.
- One task = one commit, conventional commits (`fix:`/`test:`/`docs:`).
- No GPU on this machine: Phase 2–3 steps run on Molab (manual cells, exact code below). Never paste `HF_TOKEN` into chat or commits.
- Resume semantics (pinned): `--resume hf --resume-step K` pins `total_steps` from the ckpt — the CLI cannot shorten a run, so there is no 400→600 smoke; the full resume IS the smoke, gated at step ~610.
- TDD for every CPU behavior/test change: RED verified before GREEN.

## File Structure

- `tests/test_adaptive_ckpt.py` — Phase 1 extends (P0 pins). Helpers reused: `StubTok`, `_tiny_qwen`, `_batch`, module-level `torch`, `copy`, `pytest`.
- `vision_adapter/train.py:334-361,846-872` — `_should_split` / `_n_splits_for_batch` / micro-batch loop. Phase 1 read-only; modified only if Phase 4 decides L-split.
- `vision_adapter/core.py:531-561` — `DEFAULT_L_MAX=2500`, `DEFAULT_COST_MAX=10_000_000`, `_resolve_ckpt_budget`, `ckpt_needed_for_batch`. Phase 1 read-only.
- Molab artefacts (never committed): `data/train_4000.log`, `data/probe_log.jsonl`, `data/cache/`.
- `docs/NEXT_STEPS.md §3` — spec for Phase 5 (bench harness, flex paths, kill criteria).
- This file — tick phase checkboxes on closure (commit the tick with the phase commit).

## Marquage de fin d'étape (obligatoire, chaque phase)

1. `todowrite` : phase → `completed` (une seule `in_progress` à la fois).
2. Commit du livrable (+ tick de la checkbox de phase dans ce fichier).
3. Conclusion écrite : rappel DoD chiffré, verdict GO/NO-GO explicite, prochaine phase nommée.

---

### Phase 0: Clôture grad-accum — DONE

- [x] Root cause: le micro final (`_accumulate=False`) faisait `opt.zero_grad` avant `backward` (`core.py` ancien), seule la dernière tranche contribuait au step.
- [x] Fix `bfab5a1`: `zero_grad` local seulement si `_scale == 1.0` (step standalone); la boucle `train.py:850` zéroïse une fois.
- [x] Test `test_micro_batch_accumulation_matches_full_batch`: FAIL sans fix (vérifié via stash), PASS avec.
- [x] Vérif: 102 passed, ruff clean (5 pré-existantes intactes), relecture indépendante APPROVE.
- [x] Doc fix: `_should_split` docstring `25M→10M`.

**Conclusion Phase 0:** accumulation exacte (grads à 1e-9 du full-batch). Reste connu non-bloquant: pondération micros par fraction de lignes (biais si tokens/ligne variables), branche `scaler` non couverte — follow-ups Phase 4/5 si besoin.

---

### Task 1.1: Pin P0 — aucune garantie budget à micro=1

**Files:**
- Modify: `tests/test_adaptive_ckpt.py` (append)
- Test: `tests/test_adaptive_ckpt.py`

**Interfaces:**
- Consumes: `vision_adapter.train._should_split(batch) -> bool`, `vision_adapter.train._n_splits_for_batch(B, L) -> int`, `vision_adapter.core.DEFAULT_COST_MAX` (=10M sauf override env neutralisé par monkeypatch).
- Produces: contrat verrouillé — split ne réduit que B, jamais L.

- [ ] **Step 1: Write the failing test**

```python
def test_no_budget_guarantee_at_micro_1(monkeypatch):
    from vision_adapter.core import DEFAULT_COST_MAX
    from vision_adapter.train import _n_splits_for_batch, _should_split

    monkeypatch.delenv("VISION_ADAPTER_COST_MAX", raising=False)
    # B=1 L=5000: le split se déclenche mais n_splits est cappé à B -> un seul micro plein-L
    assert _n_splits_for_batch(1, 5000) == 1
    solo = {"input_ids": torch.zeros(1, 5000), "attention_mask": torch.ones(1, 5000)}
    assert _should_split(solo) is True
    assert 1 * 5000 * 5000 > DEFAULT_COST_MAX  # toujours hors budget à micro=1
    # B=16 L=4900: 16 splits -> micro=1, chaque micro reste hors budget
    assert _n_splits_for_batch(16, 4900) == 16
    assert 1 * 4900 * 4900 > DEFAULT_COST_MAX
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/test_adaptive_ckpt.py -k "no_budget_guarantee or split_ckpt_divergence" -q`
Expected: FAIL with `no tests ran` (noms encore absents) — si FAIL d'assertion, lire le message avant de continuer.

- [ ] **Step 3: Add the test (no prod change — le contrat est déjà correct, on le verrouille)**

Append le bloc Step 1 en fin de `tests/test_adaptive_ckpt.py`.

- [ ] **Step 4: Run test to verify it passes**

Run: `pytest tests/test_adaptive_ckpt.py -k no_budget_guarantee -q`
Expected: PASS (1 passed).

- [ ] **Step 5: Commit**

```bash
git add tests/test_adaptive_ckpt.py docs/superpowers/plans/2026-09-17-probe-4000-roadmap.md
git commit -m "test(train): pin micro=1 no-guarantee budget contract"
```

### Task 1.2: Pin P0 — divergence split/ckpt sur batch long-fin

**Files:**
- Modify: `tests/test_adaptive_ckpt.py` (append)
- Test: `tests/test_adaptive_ckpt.py`

**Interfaces:**
- Consumes: `vision_adapter.core.ckpt_needed_for_batch(batch, l_max=2500, cost_max=10M) -> bool` (vrai si `L > l_max` OU `B·L² > cost_max`), `_should_split` (seulement `B·L²`).
- Produces: divergence `B=1, L=3000` verrouillée (pas de split, ckpt ON).

- [ ] **Step 1: Write the failing test**

```python
def test_split_ckpt_divergence_long_thin(monkeypatch):
    from vision_adapter.core import ckpt_needed_for_batch
    from vision_adapter.train import _should_split

    monkeypatch.delenv("VISION_ADAPTER_COST_MAX", raising=False)
    monkeypatch.delenv("VISION_ADAPTER_L_MAX", raising=False)
    thin = {"input_ids": torch.zeros(1, 3000), "attention_mask": torch.ones(1, 3000)}
    assert _should_split(thin) is False  # 9M < 10M
    assert ckpt_needed_for_batch(thin) is True  # L=3000 > L_MAX 2500
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/test_adaptive_ckpt.py -k split_ckpt_divergence -q`
Expected: FAIL (`no tests ran`, test pas encore ajouté).

- [ ] **Step 3: Add the test (no prod change)**

Append le bloc Step 1 en fin de `tests/test_adaptive_ckpt.py`.

- [ ] **Step 4: Run full gates to verify**

Run: `pytest tests/test_adaptive_ckpt.py -q`
Expected: 22 passed (20 + 2 pins).
Run: `pytest tests/ -q`
Expected: 104 passed, 1 warning.
Run: `ruff check tests/test_adaptive_ckpt.py && python -m py_compile tests/test_adaptive_ckpt.py`
Expected: `All checks passed!`, silence.

- [ ] **Step 5: Commit + push**

```bash
git add tests/test_adaptive_ckpt.py docs/superpowers/plans/2026-09-17-probe-4000-roadmap.md
git commit -m "test(train): pin split/ckpt divergence on long-thin batch"
git push origin feat/bucketed-hf-streaming
```

- [ ] **Step 6: Clôture Phase 1** — marquage §Marquage (todo, tick `- [x]` Phase 1 ci-dessous, conclusion avec chiffres 104 tests).

### Phase 1: Pins P0 CPU — DoD

- [x] 104 passed (`pytest tests/ -q`), ruff clean, push sur `feat/bucketed-hf-streaming`, HEAD noté pour le pull Molab.

---

### Phase 2: Validation GPU Molab (manuel, gated)

**Files:** aucun (lecture seule des logs). Artefacts: `data/train_4000.log`, `data/probe_log.jsonl`.

- [ ] **Step 1: Pull et vérification HEAD (cell 1)**

```python
import subprocess
print(subprocess.run(["git", "checkout", "feat/bucketed-hf-streaming"], capture_output=True, text=True).stdout)
print(subprocess.run(["git", "pull"], capture_output=True, text=True).stdout)
print(subprocess.run(["git", "rev-parse", "--short", "HEAD"], capture_output=True, text=True).stdout)
```

Expected: HEAD == commit de fin de Phase 1 (noté au push).

- [x] **Step 2: Worst-shard run (terminal, ~30-60 min)** — DONE: 50/50, `RC=0`, 0 erreur CUDA, VRAM max 12.0 Go sur emb_0099-0102.

```bash
FORCE_LARGEST_BUCKET=1 PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True,max_split_size_mb:128 \
python -m vision_adapter train --data-dir ./data --config probe --max-steps 50 --dtype auto
```

Expected: rc 0 sur les 4 shards 4901+ (`train.py:715-718`), aucune `RuntimeError` CUDA. Si OOM ici → stop, retour Phase 4 avec le log.

- [x] **Step 3: Lancement resume 4000 (cell 3 — le resume EST le smoke)** — DONE: resume HF step 400 → 401..4000, même run_id `20260913T092859Z-960abd`.

```python
def _():
    import os, subprocess

    os.chdir("/marimo/vision-adapter")
    assert os.path.exists("pyproject.toml"), "clone missing?"

    env = {
        "PATH": os.environ["PATH"],
        "HOME": os.environ.get("HOME", "/root"),
        "HF_TOKEN": os.environ.get("HF_TOKEN", ""),  # token jamais en dur ni commité
        "VISION_ADAPTER_PUSH_HF": "1",
        "VISION_ADAPTER_HF_CKPT_REPO": "keypa/vision-adapter-probe-checkpoints",
        "PYTORCH_CUDA_ALLOC_CONF": "expandable_segments:True,max_split_size_mb:128",
    }
    for k in ("CUDA_VISIBLE_DEVICES", "LD_LIBRARY_PATH"):
        if k in os.environ:
            env[k] = os.environ[k]
    assert env["HF_TOKEN"], "HF_TOKEN manquant dans l'environnement"

    os.makedirs("data", exist_ok=True)
    cmd = ["python", "-m", "vision_adapter", "train", "--data-dir", "./data",
           "--config", "probe", "--max-steps", "4000", "--dtype", "auto",
           "--push-to-hf", "--hf-ckpt-repo", "keypa/vision-adapter-probe-checkpoints",
           "--resume", "hf", "--resume-step", "400"]

    log = open("data/train_4000.log", "wb")
    proc = subprocess.Popen(
        cmd,
        stdout=log,
        stderr=subprocess.STDOUT,
        start_new_session=True,
        env=env,
    )
    return print("PID:", proc.pid)


_()
```

- [ ] **Step 4: Gate à step ~610 (le run continue en fond)**

```bash
grep -c "CUBLAS\|CUDA error\|Traceback" data/train_4000.log || true
```

Expected: `0`.

```bash
python3 -c "
import json
recs = [json.loads(l) for l in open('data/probe_log.jsonl') if l.strip()]
mem = {r['step']: r.get('mem_reserved_gb') for r in recs if r.get('type') == 'train' and r['step'] in (475, 600)}
print('steps vus:', recs[-1]['step'], 'reserved@475/600:', mem)
assert recs[-1]['step'] >= 610, 'run pas encore à 610'
assert all(v is None or v < 80 for v in mem.values()), mem
steps = [r['step'] for r in recs if r.get('type') == 'train']
assert 475 in steps, 'step 475 (killer emb_0089) manquant'
print('GATE GPU OK')
"
```

- [x] **Step 5: Clôture Phase 2** — GO. Step 475 passé (loss 1.383), step 600 passé, 0 erreur CUDA sur tout le run.

### Phase 2: Validation GPU — DoD

- [x] Worst-shard rc 0 + gate vert sur les critères porteurs : 0 erreur CUDA, step 475 passé.
- [x] Nuance documentée : `mem_reserved_gb` a atteint 80-94 sur 400-620 (seuil <80 du plan non tenu), MAIS `mem_alloc_gb` constant à 3.71 Go — c'est de la rétention allocator, pas des tenseurs vivants (retombe à ~12 aux points de save avec `empty_cache`). Le seuil <80 était mal calibré comme proxy ; 0 OOM sur 4000 steps tranche.

---

### Phase 3: Probe 4000 (manuel)

- [ ] **Step 1: Laisser continuer + watchdog (garde l'onglet ouvert)**

```python
import time
for i in range(144):
    time.sleep(300)
```

12h de keep-alive (shutdown idle 90 min sinon).

- [ ] **Step 2: Suivi `tail -f data/train_4000.log`, checkpoint HF `projector_stepK.pt` + `probe_log.jsonl` poussés à chaque save.**
- [ ] **Step 3: Vérification grok + courbes**

```bash
python3 -c "
import json
recs = [json.loads(l) for l in open('data/probe_log.jsonl') if l.strip()]
tr = [r for r in recs if r.get('type') == 'train']
print('steps:', tr[-1]['step'], 'samples:', tr[-1]['samples_seen'], 'ema:', tr[-1].get('ema_loss'))
assert tr[-1]['step'] >= 4000 and tr[-1]['samples_seen'] >= 57600  # 3600x16 fenetre grok
"
python scripts/plot_probe.py --input data/probe_log.jsonl --output probe_beautiful_4000.html  # flags --input/--output, voir --help si diffère
```

- [x] **Step 4: Clôture Phase 3** — 4000/4000, `run_end` step 4000, 64000 samples, loss finale 1.12373, EMA 1.0233, wall 397.8 min (~6.6h), 0 erreur CUDA. HF: `projector_step4000.pt` + `projector_final_4000.pt` + `probe_log.jsonl` (3600 recs 401→4000 contigus) + `train_4000.log` + `probe_curves.png` (pas le html `probe_beautiful_*`, courbes tout de même générées).

### Phase 3: Probe 4000 — DoD

- [x] `4000/4000` rc 0, `samples_seen = 64000 ≥ 57600` (111 % fenêtre grok), ckpts + log sur `keypa/vision-adapter-probe-checkpoints`, courbes générées.

---

### Phase 3b: Gate grok — held-out + génération sur ckpt final (Molab)

Train loss plate 401→4000 (EMA ~1.0-1.2, pas de collapse à 3600) : le grok se juge en held-out, pas sur cette courbe.

- [x] **Script** `scripts/eval_heldout.py` (+ `scripts/__init__.py`) : `forward_loss` (même path sélectif que le train, sans backward), `select_heldout_rows` (shards `emb_0000/0001`, jamais entraînés, déterministe). Tests `tests/test_eval_heldout.py` : égalité loss vs `train_step_qwen`, déterminisme, exclusion — 107 tests verts, ruff clean.
- [ ] **Run Molab** (même bootstrap, terminal) :

```bash
cd /marimo/vision-adapter && git pull && export HF_TOKEN="TON_VRAI_TOKEN" && python scripts/eval_heldout.py --n 60 --data-dir ./data 2>&1 | tail -n 20
```

Expected : JSON avec `rel_improvement`, gate `rel_improvement >= 0.10`.
- [ ] **Générations** (même session, ckpts déjà téléchargés par l’eval dans le cache HF) :

```bash
cd /marimo/vision-adapter && export HF_TOKEN="TON_VRAI_TOKEN" && F=$(python3 -c "from huggingface_hub import hf_hub_download; print(hf_hub_download('keypa/vision-adapter-probe-checkpoints','projector_final_4000.pt'))") && B=$(python3 -c "from huggingface_hub import hf_hub_download; print(hf_hub_download('keypa/vision-adapter-probe-checkpoints','projector_step400.pt'))") && python scripts/colab_unsloth_test.py --ckpt "$F" --prompt "Describe the image." && python scripts/colab_unsloth_test.py --ckpt "$B" --prompt "Describe the image."
```

Expected : sortie `Gen:` non-dégénérée pour le final, comparaison qualitative vs base.
- [ ] **Verdict** : GO Phase 5 si `rel_improvement >= 0.10` ET générations cohérentes ; sinon on étend l’eval (plus de rows, autre baseline random) avant tout bench.

### Phase 3b: Gate grok — DoD

- [ ] `rel_improvement >= 0.10` sur 60 rows held-out + générations final vs base comparées.

---

### Phase 4: Décision L-split (données Phase 2, pas avant)

Table de décision (appliquer dans l'ordre, premier match gagne) :

| Observation Phase 2 | Décision | Action exacte |
|---|---|---|
| Gate vert partout | Rien à faire | Clôturer, Phase 5 |
| OOM/CUBLAS sur `L≈800` avec split actif | Seuil trop haut | Relancer avec `VISION_ADAPTER_COST_MAX=5000000` dans `env` cell 3 (pas de code), retour Phase 2 Step 3 |
| OOM sur `micro=1`, `L>3162` (seul ckpt ON) | Vrai P0 | Écrire plan L-split (découpage séquence) en TDD, puis implémenter ; retour Phase 2 |
| OOM ailleurs / réservé qui rampe | Fuite suspecte | `memory_summary` au site + ouvrir investigation systématique, pas de fix aveugle |

- [x] **Step 1: Appliquer la table, noter l'observation (steps, L, bl2, log).** — Premier match : « Gate vert partout » → rien à faire. Pire cas (0099-0102, micro=1, ckpt ON) passé en RC=0 ; killer L≈800 passé en split 2×8 ; 0 OOM sur 4000 steps.
- [x] **Step 2: Clôture Phase 4** — Pas de L-split : non justifié par les données. Reste : commit/merge du run, puis Phase 5.

---

### Phase 5: Bench hf_transfer vs Range + FlexAttention prep

Spec : `docs/NEXT_STEPS.md §3` (R1/R2, micro-bench apportionment, kill : flex <1.5× le temps full-layer ou toute divergence → on garde bucketing+ckpt).

- [ ] **Step 1: Bench transferts** — même shard, `HF_HUB_ENABLE_HF_TRANSFER=1` (whole-shard) vs `_fetch_range` (Range) : MiB/s + retries ; consigner dans `docs/NEXT_STEPS.md`.
- [ ] **Step 2: Micro-bench flex** — unit (flex-vs-eager 2 couches, fwd allclose 1e-2 bf16), intégration, soak 1h worst-bucket p99 ±15 %, VRAM flat ; étapes §3.4 de NEXT_STEPS.
- [ ] **Step 3: Décision flex** — appliquer le kill criteria, consigner.
- [ ] **Step 4: Clôture Phase 5 + clôture roadmap** — marquage §Marquage, tick final ci-dessous.

### Phase 5: Bench — DoD

- [ ] Chiffres transferts + verdict flex (GO/NO-GO avec mesures) consignés, roadmap entièrement tickée.
